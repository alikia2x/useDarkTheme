declare module "@alikia/dark-theme-hook" {
    /**
     * A custom React hook to detect the user's preferred color scheme (dark mode).
     * @returns {boolean} - A boolean indicating whether dark mode is enabled.
     */
    function useDarkMode(): boolean;

    export default useDarkMode;
}
